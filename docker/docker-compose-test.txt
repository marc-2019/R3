version: '3.8'

services:
  test-db:
    image: postgres:14-alpine
    platform: windows/amd64  # Change platform specification
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: r3_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  test-redis:
    image: "redis:latest"  # Change to latest tag
    platform: windows/amd64  # Change platform specification
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  root-node-test:
    build:
      context: ./docker/root-node
      dockerfile: Dockerfile.test
    platform: linux/amd64  # Specify platform
    ports:
      - "8546:8545"

  reality2-test:
    build:
      context: ./docker/reality2
      dockerfile: Dockerfile.test
    platform: linux/amd64  # Specify platform
    ports:
      - "3002:3001"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@test-db:5432/reality2_test